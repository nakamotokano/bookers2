<!-- 切り取った -->
<div class="container px-5 px-sm-0">
  <div class="row">
    
<%= render 'list', user: @user , book: @book_new %>
<div class="col-md-8 offset-md-1">
<h1>Book detail</h1>

<table class="table">
   <tbody>
 
  <tr><td>
    <p>
  
          <%= image_tag @user.get_profile_image(100,100) %>
      </p>
  <%= link_to @user.name , user_path(@user.id) %>
  </td><td><%= link_to @book.title, book_path(@book.id) %></td>
  <td><%= @book.body %></td>
    <% if @book.user == current_user %>
  <td>
    <%= link_to edit_book_path(@book.id) ,class:"btn btn-sm btn-success" do %>Edit
  <% end %>
  </td>
  <td>
    <%= link_to book_path(@book.id),method: :delete, "data-confirm" => "本当に削除しますか？" ,class: "btn btn-sm btn-danger" do%>
    Destroy <% end %></td>
  </tr>
    <% end %>
</tbody></table>
    </div>
     </div>
</div>